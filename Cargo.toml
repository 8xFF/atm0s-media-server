[workspace]
members = [
    "packages/cluster",
    "packages/endpoint",
    "packages/transport",
    "packages/media-utils",
    "clusters/local",
    "clusters/sdn",
    "transports/webrtc",
    "transports/rtmp",
    "transports/sip",
    "servers/media",
    "servers/sip-gateway",
]

[workspace.dependencies]
cluster = { path = "packages/cluster", version = "0.1.0" }
endpoint = { path = "packages/endpoint", version = "0.1.0" }
transport = { path = "packages/transport", version = "0.1.0" }
transport-webrtc = { path = "transports/webrtc", version = "0.1.0" }
transport-rtmp = { path = "transports/rtmp", version = "0.1.0" }
transport-sip = { path = "transports/sip", version = "0.1.0" }
cluster-local = { path = "clusters/local", version = "0.1.0" }
cluster-sdn = { path = "clusters/sdn", version = "0.1.0" }
media-utils = { path = "packages/media-utils", version = "0.1.0" }
futures = "0.3"
async-trait = "0.1"
async-std = { version = "1.12", features = ["tokio1", "unstable", "attributes"] }
async-notify = "0.2"
parking_lot = "0.12"
log = { version = "*" }
env_logger = { version = "*" }
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }